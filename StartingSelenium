import java.util.list;

import org.openga.selenium.By;
import org.openga.selenium.WebDriver;
import org.openga.selenium.WebLement;
import org.openga.selenium.chrome.ChromeDriver;

public class ExempleOfProperties 
{
  system.setProperty("webdriver.chrome.driver", "D:\\pathtochromedriver");
  WebDriver driver = new ChromeDriver();
  driver.get("http://somewebside");
}
